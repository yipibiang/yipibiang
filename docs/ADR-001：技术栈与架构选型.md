# ADR-001：技术栈与架构选型

> 状态：已批�?| 日期�?026-04-29



***

## 一、决策摘�?
本项目采�?**Spring Boot 微服�?+ Vue 3 前端 Monorepo** 架构，后端以六边形架构保证领域逻辑纯净，前端以 pnpm workspace 管理�?app 共享类型�?


***

## 二、技术选型

### 2.1 后端技术栈



| 分类     | 技�?                        | 版本          | 决策理由                               |
| ------ | -------------------------- | ----------- | ---------------------------------- |
| 运行�?   | JDK                        | 21 (LTS)    | Spring Boot 3.x 最低要�?17，LTS 确保长期支持 |
| 框架     | Spring Boot                | 3.2.x       | 稳定优先，不追新                           |
| 框架     | Spring Cloud               | 2023.0.x    | �?Spring Boot 3.2 配套，版本兼�?         |
| 网关     | Spring Cloud Gateway       | 4.1.x       | 统一入口�?080），路由转发 + 鉴权               |
| ORM    | MyBatis                    | 3.0.x       | SQL 完全可控，复杂查询优�?JPA                |
| 对象映射   | MapStruct                  | 1.5.5.Final | 编译期生成，零运行时开销                       |
| 数据�?   | MySQL                      | 8.2         | 关系型主存，事务可靠                         |
| 缓存     | Redis                      | 7.2         | Session 共享 + 行级缓存                  |
| 消息队列   | RabbitMQ                   | 3.13.x      | 本地轻量；六边形架构消息端口允许随时替换�?Kafka        |
| 数据库迁�? | Flyway                     | 10.x        | SQL 版本化管理，支持回滚                     |
| 安全     | Spring Security            | 6.2.x       | 统一认证授权                             |
| 认证     | jjwt                       | 0.12.x      | 无状�?JWT，适配横向扩展                     |
| 日志     | Logback + Logstash Encoder | 7.4         | JSON 格式，兼�?Loki 聚合                 |
| 链路追踪   | OpenTelemetry              | 1.35.x      | 统一埋点，跨服务 traceId 串联                |
| API 文档 | SpringDoc OpenAPI          | 2.3.x       | Swagger UI 前置开�?                   |
| 构建     | Maven                      | 3.9.x       | 统一构建                               |

### 2.2 前端技术栈



| 分类       | 技�?               | 版本      | 决策理由              |
| -------- | ----------------- | ------- | ----------------- |
| 运行�?     | Node.js           | 22.11.0 | 前端编译环境            |
| 框架       | Vue               | 3.4.x   | 组合�?API，渐进增�?     |
| 构建       | Vite              | 5.x     | HMR 快，开发体验好       |
| 语言       | TypeScript        | 5.4.x   | 类型安全，编译期排除隐患      |
| 组件�?     | Element Plus      | 2.7.x   | Vue 3 成熟组件�?      |
| Monorepo | pnpm workspace    | 9.x     | 磁盘复用快，lockfile 可靠 |
| 类型共享     | OpenAPI Generator | 7.x     | 前后端类型同源，消除手写错误    |

**决策**：前端类型定义通过 OpenAPI Generator 从后�?`.yaml` 自动生成�?`packages/shared-types`，禁止手动修改�?
### 2.3 基础设施



| 分类   | 技�?                            | 说明                |
| ---- | ------------------------------ | ----------------- |
| 容器�? | Docker Compose 2.x             | 本地编排              |
| 反向代理 | Traefik 3.0                    | 自动发现容器，HTTPS 终结   |
| 链路追踪 | Jaeger 1.55                    | 分布式追�?UI          |
| 监控   | Prometheus 2.50 + Grafana 11.x | 指标采集与可视化          |
| 日志聚合 | Loki 2.9.x                     | 日志收集，与 Grafana 集成 |
| CI   | GitHub Actions                 | push 后自动构建测�?     |
| 镜像仓库 | GHCR (ghcr.io)                 | 免费，私有仓库安�?        |



***

## 三、架构决�?
### 3.1 微服务：六边形架�?
**决策**：每个业务微服务采用六边形架构（Hexagonal Architecture），分为四层�?


| 层级               | 职责                            | 约束                |
| ---------------- | ----------------------------- | ----------------- |
| `interfaces`     | REST Controller、MQ Consumer   | 只依�?`application` |
| `application`    | UseCase、DTO、Service 接口        | 只依�?`domain`      |
| `domain`         | Entity、Repository 接口、VO、Event | **零依�?*，是架构核心     |
| `infrastructure` | Mapper 实现、MQ 发送器、外部适配�?       | 实现 `domain` 层接�?  |

**决策理由**�?


* 领域逻辑完全脱离框架，可脱离容器单独测试

* 基础设施可替换（换数据库 / 消息队列），不影响核�?
* Repository 接口定义�?`domain`，实现在 `infrastructure`，严格依赖倒置

**关键约束**：六边形架构是架构级红线，违反者代码审查不通过�?
### 3.2 共享包策�?
**决策**：`packages/` 下所有模块必须满足：



* 无业务逻辑：仅数据类型、工具函�?
* 无框架依赖：禁止引入 Spring、MyBatis �?
**决策理由**：避免服务间依赖冲突；前后端共同引用时无技术栈障碍�?
### 3.3 Monorepo 结构



```
apps/                # 可运行产�?
├── api-gateway/     # 网关服务�?080�?
├── services/
│   └── user-service/
│       ├── src/
│       ├── pom.xml
│       └── deployments/
│           └── Dockerfile  <-- 【在这里】
�?  └── order-service/ # 订单服务（第二阶段）

├── workers/         # 异步任务

�?  └── report-worker/ # 报表生成

├── admin/           # 后台管理前端

└── web/             # C 端前�?
packages/            # 共享库（禁业务逻辑�?
├── types/           # 纯类型定�?
├── schemas/         # OpenAPI 协议

└── utils/           # 工具函数

platform/            # 基础设施配置

├── docker-compose.\*.yml # 编排文件

└── scripts/         # 部署脚本
```

### 3.4 消息中间件选择

**决策**：本地开发使�?RabbitMQ，生产环境可按需切换消息中间件�?
**决策理由**：RabbitMQ 轻量、本地资源消耗低；六边形架构的消息端口（`infrastructure/messaging/`）封装了消息发送逻辑，切换为 Kafka 时无需修改领域和应用层代码�?


***

## 四、安全策�?
**决策**�?


* 认证：无状�?JWT（HS256），`access_token` 有效�?30 分钟，`refresh_token` 有效�?7 �?
* 密码：BCrypt 加密（strength = 10），加盐存储

* JWT 密钥：从环境变量 `JWT_SECRET` 读取，密钥长�?�?256 bits

* 敏感信息：密码、Token 禁止写入日志，接口返回屏蔽敏感字�?
**放行接口**（无需认证）：



* `GET /actuator/health`（健康检查）

* `GET /actuator/prometheus`（监控指标）

* `GET /swagger-ui/**`（API 文档�?
* `GET /v3/api-docs/**`（OpenAPI 协议�?
* `POST /auth/login`（用户登录）

* `POST /auth/register`（用户注册）



***

## 五、环境策�?
**决策**�?


1. 环境隔离：通过 `.env.dev`（本地开发）�?`.env.prod`（生产部署）区分环境变量

2. 本地开发：启动核心中间件（MySQL + Redis + RabbitMQ），可观测性栈按需启动

3. 生产部署：完整部署含 Traefik 反向代理、可观测性栈（Jaeger + Prometheus + Grafana + Loki�?
4. 密钥管理：生产环境密码、密钥由部署平台运行时注入，`application.yml` 中用 `${VAR:default}` 引用

5. Git 约束：禁止提�?`.env` 文件到仓库，仅允许提�?`.env.example` 模板文件

**决策理由**�?


* 环境隔离杜绝本地配置污染生产

* 密钥不进代码仓库，消除泄露风�?
* 容器化交付保证开发与生产环境一致�?


***

## 六、禁止事项（架构红线�?
以下事项全生命周期严格禁止，代码审查时直接驳回：



1. 代码中硬编码密钥、密码、Token 等敏感信�?
2. �?JWT 中存放密码、手机号、身份证号等敏感个人信息

3. 接口返回明文密码或未脱敏的敏感字�?
4. 密钥未从环境变量�?`.env` 文件读取（允许本地开发用默认值兜底）

5. �?`packages/` 中添加业务逻辑或框架依�?
6. 违反六边形架构的依赖方向（如 domain 层依�?infrastructure 层）

7. 提交 `.env` 文件�?Git 仓库

8. 手动修改 `packages/shared-types` 中的自动生成类型



***

## 七、决策原�?


| 原则    | 含义                                           |
| ----- | -------------------------------------------- |
| 生产可用  | 所有技术组件均经过大规模生产环境验证，拒绝小众未成熟方�?                |
| 本地可跑  | 个人开发电脑可流畅运行完整开发环境，无需高配设备                     |
| 架构清晰  | 以六边形架构为核心，分层职责明确，保证长期可维护�?                   |
| 安全基线  | 遵循「JWT 认证 + 密钥不进仓库 + 敏感信息脱敏」的安全底线            |
| 分阶段交�?| 先完�?User 服务全链路（注册 - 登录 - 查询），再叠加消息队列、可观测性等功能 |
